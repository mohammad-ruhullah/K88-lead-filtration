# AGENTS.md

## Project Objective
Build a Vite + React + TypeScript + Tailwind web app that:
1) runs DuckDB-Wasm in the browser,
2) loads user-selected very large CSV files (4+ files supported),
3) filters data based on user criteria,
4) produces one final merged dataset using UNION semantics,
5) exports result without server-side processing.

## Non-Negotiable Constraints
- All data ingestion, filtering, union, and export must run in the user's browser.
- Prioritize efficiency and memory safety for million-row CSV inputs.
- Do not send dataset rows to any backend service.

## Huge File Safety Disclaimer (MANDATORY)
- DO NOT EVER TRY TO READ THESE FILES FULL.
- For inspection/debugging, only read tiny slices (e.g. first 5-50 lines).
- Use small previews only (`Read` with low limit or `head -n` style command).
- Never run commands that print full file contents.
- In app code, avoid loading full CSV content into JS strings/arrays when file handles can be registered directly with DuckDB-Wasm.

## Source Datasets (Current)
- User will pick files from local machine at runtime (do not assume fixed filesystem paths).
- Current sample files used during development:
- `From_500_To_Beyond_1_of_4.csv`
- `From_500_To_Beyond_2_of_4.csv`
- `From_500_To_Beyond_3_of_4.csv`
- `From_500_To_Beyond_4_of_4.csv`

## Confirmed Data Strategy
- Combine datasets with UNION (not JOIN).
- Default union operator: `UNION ALL BY NAME` to preserve all rows and tolerate schema drift.
- Support dynamic number of input files (not fixed to exactly 4).
- If deduplication is later required, add an optional dedupe stage as a separate feature.

## Architecture Direction
- DuckDB-Wasm service layer for:
  - instantiate DB/worker once,
  - manage a single long-lived connection,
  - register user-selected local CSV file handles,
  - execute SQL queries for filtering/union,
  - export result files via `COPY ... TO` + `copyFileToBuffer`.
- React UI layer for:
  - file status,
  - filter builder,
  - query preview,
  - export action.
- SQL-first data pipeline (push filtering and union into DuckDB, not JS loops).

## Iterative Feature Roadmap
1. Foundation: DuckDB service + lifecycle states.
2. File registration: load N CSV file handles from file picker and validate schema/header.
3. Filter model: typed criteria and SQL generation.
4. Union pipeline: create merged filtered result table/view.
5. Preview: sampled result rows and row counts.
6. Export: download final merged dataset as CSV.
7. Performance hardening: memory/thread tuning, cleanup, and UX improvements.

## Done Criteria Per Feature
- Feature code is implemented and wired to UI.
- Large-file safety rules remain respected.
- No server dependency is introduced.
- Basic manual validation steps are documented in this file.
- AGENTS.md is updated with decisions, risks, and next step.

## Decision Log
- 2026-04-21: Created AGENTS.md as project control file.
- 2026-04-21: Confirmed union-based merge strategy.
- 2026-04-21: Added mandatory huge-file disclaimer and dataset inventory.
- 2026-04-21: Implemented Feature 1 with singleton DuckDB runtime service and app-level lifecycle UI states (`idle`, `initializing`, `ready`, `error`).
- 2026-04-21: Foundation keeps one long-lived DuckDB connection per app session.
- 2026-04-21: Updated requirement to support user-selected local files (dynamic count: 4/5/6+).
- 2026-04-21: Implemented Feature 2 with multi-file picker, small-slice header validation, and DuckDB file-handle registration.
- 2026-04-21: Implemented filter criteria model with fixed rule `CURRENT_CASH_BALANCE >= 5000`.
- 2026-04-21: Materialized merged filtered result as temp view `merged_filtered_dataset` using UNION-compatible multi-file read (`union_by_name = true`).
- 2026-04-21: Implemented CSV-only export from temp view via `COPY ... TO` + `copyFileToBuffer` and browser download.
- 2026-04-21: Switched filter pipeline from prepared parameters to direct SQL execution due DuckDB-Wasm binder limitation (`Unexpected prepared parameter` for this query shape).
- 2026-04-21: Added cash-only property policy (client requirement). Filtration now excludes securities (`SC*`), safe-deposit/tangible (`SD*`), non-standard security codes, and any type that cannot be verified as cash. Rule is always-on, generated by `src/services/propertyTypePolicy.ts`, applied inside the `merged_filtered_dataset` materialize SQL, and cross-checked against `SHARES_REPORTED`/`CUSIP`/`NAME_OF_SECURITIES_REPORTED` evidence columns. Filtration hard-blocks when `PROPERTY_TYPE` column is missing. UI surfaces a `Skipped Non-Cash` metric and a Type column in preview.
- 2026-04-21: Data analysis of sample CSVs (files 2-4, 2.76M records, proper CSV parsing): of the 300,757 balance-qualified rows (>= $5000), the cash-only rule rejects 83,441 (83,388 by property type, 53 by securities evidence columns) and keeps 217,316 qualified leads with zero securities/safe-deposit leakage. Without the rule, those 83,441 non-cash claims would have become leads.

- 2026-08-24: Reworked the filtration rules to be **owner-centric** instead of property-centric (client requirement). Seven confirmed rules:
  1. **Claim gate runs first** - only `NUMBER_OF_PENDING_CLAIMS = 0` AND `NUMBER_OF_PAID_CLAIMS = 0` survive. NULL/unparsable counts as claimed.
  2. **Owner identity** = exact `OWNER_NAME` + exact address (`OWNER_STREET_1/2/3`, `OWNER_CITY`, `OWNER_STATE`, `OWNER_ZIP`, `OWNER_COUNTRY_CODE`). Normalisation is trim + uppercase + whitespace-collapse only; parts joined with `chr(31)` so values cannot bleed across fields.
  3. **One property with several co-owners is left as-is** - those rows are never merged or collapsed.
  4. **Small properties ride along** - a qualifying owner exports *all* their properties, nothing dropped for being small.
  5. **Airtable dedupe runs BEFORE summing** - an already-worked property cannot help an owner reach the threshold.
  6. **Rows without a street address are dropped**, so unrelated same-name people can never collapse into one bogus owner group.
  7. **Output is grouped** - an owner's properties occupy consecutive rows, richest owner first.
- 2026-08-24: Owner totals sum over **DISTINCT `PROPERTY_ID`**, not rows. A jointly held property emits one row per co-owner sharing a `PROPERTY_ID`, so summing rows would invent money that does not exist. Verified: 92 owner-groups in a single 60MB slice have a repeated `PROPERTY_ID`.
- 2026-08-24: Pipeline is now three statements - `eligible_rows` (one CSV pass: all row-level gates + anti-join), `owner_totals` (distinct-property aggregation), `merged_filtered_dataset` (threshold + ordering). The CSVs are still parsed once.
- 2026-08-24: Replaced the dedicated non-cash re-scan with a single conditional-aggregate funnel query (`total / skipped_claimed / skipped_no_address / skipped_non_cash`), removing a full extra scan of every CSV.
- 2026-08-24: Export re-states `ORDER BY` inside the `COPY` (a materialised table does not guarantee scan order) and adds `OWNER_GROUP_ID`, `OWNER_GROUP_PROPERTY_COUNT`, `OWNER_GROUP_TOTAL` alongside all 25 original columns. Internal `__`-prefixed columns are excluded.
- 2026-08-24: Extracted shared `src/services/columnResolver.ts` (`matchColumn`, `quoteColumn`, `envField`), removing the duplicate implementations in `propertyTypePolicy.ts` and `App.tsx`. Deleted dead `buildUnionSourceSQL_Simple`.
- 2026-08-24: Verified against real data (60MB mid-file slice, 283,616 rows) by running the generated SQL through DuckDB directly: 15,220 skipped as claimed, 24,090 skipped for no address, 132,469 skipped as non-cash, leaving **120 qualifying owners across 706 properties - all 120 of which qualified *only* by combining properties** (largest: AUSTIN DIAGNOSTIC, 19 properties of ~$1,599 each totalling $29,205). Zero rows in this region qualify individually under the old per-property rule.
- 2026-08-24: Threshold remains `>= 5000` (exactly $5,000.00 qualifies), now applied to the owner's combined total rather than a single property. UI relabelled "Minimum Owner Total".

- 2026-08-25: Added the **filtration report** (client requirement: "for which reason how many data filtered out"). On-screen panel only, full sub-reason breakdown, row counts only, dataset totals only. Rendered by `src/components/FiltrationReport.tsx` beneath the metric tiles.
- 2026-08-25: Two rejection reasons were previously **never counted at all**: rows removed by the Airtable dedupe, and rows removed because their owner's total fell under the threshold. The threshold bucket turned out to be the single largest reason in the dataset (111,131 of 283,616 rows - 39% - on the verification slice).
- 2026-08-25: The report is built on a **reconciliation invariant**: `rows scanned - sum(all reasons) === qualified`. Reasons are mutually exclusive because every `FILTER` is conditioned on passing all *prior* stages. The panel renders a `Reconciled` / `Mismatch` badge and, on mismatch, an explicit warning that the counts are unreliable - a silent wrong number is worse than a visible one.
- 2026-08-25: `funnelSQL` now evaluates each predicate exactly **once per row** in a `flags` subquery, then aggregates over the booleans. Repeating the predicates inside a dozen `FILTER` clauses would re-run `regexp_matches` a dozen times per row across millions of rows.
- 2026-08-25: The report's CRM check uses `EXISTS`, **not** a `LEFT JOIN`. Verified: with a duplicated `(PROPERTY_ID, OWNER_NAME)` pair in the lookup, a `LEFT JOIN` reported 283,617 scanned rows against a true 283,616, while `EXISTS` stayed correct. `createAirtableLookupSQL` also now builds the lookup with `SELECT DISTINCT` to remove the hazard at source.
- 2026-08-25: Split the policy builders into their individual predicates - `buildPropertyTypeFilterParts()` (`propertyTypePolicy.ts`) and `buildClaimGateParts()` (`ownerGroupingPolicy.ts`). The existing `build*SQL` functions now compose from those parts, so the report's attribution can never drift from the filter that is actually applied. Verified the refactored cash-only filter is **identical on all 283,616 rows** (0 disagreements) against the original expression.
- 2026-08-25: Report cost is **no extra pass over the CSVs**. The funnel query already scanned once; the threshold and owner-level counts run on the already-materialised `eligible_rows` / `owner_totals` tables.
- 2026-08-25: Measured funnel on the 60MB verification slice - claimed 15,220 (pending 8,159 / paid 7,061), no address 24,090, non-cash 132,469 (denied keyword 87,085 / not a cash code 45,155 / shares 222 / CUSIP 7), in CRM 0, under threshold 111,131, qualified 706. Note 229 rows are caught **only** by the securities-evidence cross-checks after passing the property-type test - previously invisible inside the non-cash lump.
- 2026-08-25: `FunnelStats` was replaced by `FiltrationReportData`; the four summary tiles now derive from the same report object via `summarise()`, so tiles and report cannot disagree.

## Basic Manual Validation (Feature 1)
1. Run `pnpm dev`.
2. Open the app in browser.
3. Confirm status moves from `Initializing` to `Ready`.
4. Confirm a DuckDB version string appears in the runtime card.
5. Confirm no network/backend API is required for initialization.

## Basic Manual Validation (Feature 2)
1. Run `pnpm dev`.
2. In the app, click `Select CSV Files` and choose 4+ matching CSV files from any folder.
3. Confirm status becomes `Registered` and file count matches selection.
4. Confirm virtual file paths appear in the file registration list.
5. Try selecting one file with a different header and confirm schema mismatch is reported.

## Basic Manual Validation (Feature 3/4/5)
1. After registering files, click `Run Filter`.
2. Confirm filter rule shown is `CURRENT_CASH_BALANCE >= 5000`.
3. Confirm status moves to `Ready` and a filtered row count is displayed.
4. Confirm preview rows are shown (limited sample, not full dataset materialization in JS).
5. Confirm message states temp view `merged_filtered_dataset` is ready.

## Basic Manual Validation (Feature 6)
1. After filter status is `Ready`, click `Download CSV`.
2. Confirm a `.csv` file downloads in browser.
3. Open the downloaded file and confirm rows satisfy `CURRENT_CASH_BALANCE >= 5000`.

## Basic Manual Validation (Cash-Only Property Policy)
1. After registering files, confirm the `Cash-Only Policy (Always On)` badge appears in the Filtration Logic card.
2. Click `Run Filter`.
3. Confirm the `Skipped Non-Cash` metric shows a positive count (for the sample files, expect roughly the securities + safe-deposit totals).
4. Confirm preview rows show a `Type` column and every value is a cash-type (no `SC*`/`SD*`/safe-deposit values).
5. Download CSV and confirm no row has a securities or safe-deposit `PROPERTY_TYPE`.
6. Try registering a CSV without a `PROPERTY_TYPE` column and confirm filtration is blocked with a clear error.

## Basic Manual Validation (Owner-Group Filtration)
1. Register the sample CSVs and click `Start Filtration`.
2. Confirm the Filtration Logic card shows three always-on badges: `Unclaimed Only`, `Cash-Only Policy`, `Owner Grouping`.
3. Confirm the funnel tiles populate: `Rows Scanned`, `Skipped Claimed`, `Skipped No Address`, `Skipped Non-Cash`.
4. Confirm `Qualifying Owners` and `Unlocked by Grouping` show positive counts.
5. In the preview, confirm rows of the same owner are banded together, the first row carries an `N props` badge and the `Owner Total`, and following rows read `> same owner`.
6. Download the CSV and confirm:
   - every row has `NUMBER_OF_PENDING_CLAIMS = 0` and `NUMBER_OF_PAID_CLAIMS = 0`;
   - no row has a blank `OWNER_STREET_1`;
   - rows sharing an owner name + address are contiguous, richest owner first;
   - `OWNER_GROUP_TOTAL` equals the sum of that owner's **distinct** `PROPERTY_ID` balances, and is always >= the threshold;
   - a group exists whose individual properties are all under $5,000.
7. Add one property of a qualifying owner to the Airtable table, re-run, and confirm that owner's total drops by that amount - and that the group disappears entirely if it falls below the threshold.
8. Register a CSV lacking `NUMBER_OF_PAID_CLAIMS` and confirm filtration is blocked with a clear error rather than silently skipping the rule.

## Basic Manual Validation (Filtration Report)
1. Register the sample CSVs and click `Start Filtration`.
2. Confirm a `Filtration Report` panel appears beneath the metric tiles with a green `Reconciled` badge.
3. Confirm the waterfall lists every reason in pipeline order with a `Remaining` column that steps down to the qualified figure, and that sub-reasons are indented beneath `Already claimed` and `Non-cash property`.
4. Confirm the `QUALIFIED` figure equals the `Qualified Properties` tile and the exported CSV's row count.
5. Confirm the owner-level strip shows groups formed / qualified / under threshold / unlocked by grouping, and that qualified + under threshold == groups formed.
6. Add a known property to the Airtable table, re-run, and confirm `Already in Airtable` moves off zero while the badge stays `Reconciled`.
7. Feed a CSV whose claim columns hold a non-numeric value and confirm those rows land in `unreadable claim value` rather than vanishing silently.

## Next Feature To Build
- Feature 7: performance hardening and memory-focused UX safeguards. Note the new
  `eligible_rows` temp table is an additional materialised intermediate - measure peak
  browser memory across the full 4-file set before shipping to production.
